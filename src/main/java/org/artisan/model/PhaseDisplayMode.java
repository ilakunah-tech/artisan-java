package org.artisan.model;

/**
 * Phase display mode for each roast phase LCD (Artisan Config » Phases).
 * Right-click on Phases LCD cycles: TIME → PERCENTAGE → TEMPERATURE.
 */
public enum PhaseDisplayMode {
    TIME,
    PERCENTAGE,
    TEMPERATURE
}
